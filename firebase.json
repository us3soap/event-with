{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "manifest.json",
        "destination": "/manifest.json"
      }
    ]
  }
}
